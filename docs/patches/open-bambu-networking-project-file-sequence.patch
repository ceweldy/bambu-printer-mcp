diff --git a/include/obn/print_job.hpp b/include/obn/print_job.hpp
index b5e4ae6..92614b1 100644
--- a/include/obn/print_job.hpp
+++ b/include/obn/print_job.hpp
@@ -86,4 +86,9 @@ struct ProjectFileOpts {
 std::string build_project_file_json(const BBL::PrintParams& p,
                                     const ProjectFileOpts&  opts);
 
+// Returns the project_file sequence generated by the latest build on the
+// calling thread. The Linux bridge calls this immediately after a synchronous
+// print ABI returns so it can correlate the printer acknowledgement exactly.
+std::string last_project_file_sequence_id();
+
 } // namespace obn::print_job
diff --git a/src/print_job.cpp b/src/print_job.cpp
index 561440b..efc4ab3 100644
--- a/src/print_job.cpp
+++ b/src/print_job.cpp
@@ -17,6 +17,7 @@
 // buying us anything.
 
 #include "obn/agent.hpp"
+#include "obn/abi_export.hpp"
 #include "obn/print_job.hpp"
 
 #include "obn/bambu_networking.hpp"
@@ -77,6 +78,8 @@ std::string to_print_basename(std::string fname)
 
 namespace {
 
+thread_local std::string g_last_project_file_sequence_id;
+
 // Strict-enough JSON string escaper. The printer firmware's parser is
 // lenient but we still need to handle quotes/backslashes/control bytes
 // so that, e.g., project names containing quotes don't break the wire
@@ -349,9 +352,11 @@ std::string build_project_file_json_impl(const BBL::PrintParams& p,
     std::string bed_type    = p.task_bed_type.empty() ? "auto" : p.task_bed_type;
     std::string ams_mapping = format_ams_mapping(p.ams_mapping, p.task_use_ams);
 
+    g_last_project_file_sequence_id = now_seq_id();
+
     std::ostringstream os;
     os << "{\"print\":{";
-    os << "\"sequence_id\":" << json_escape(now_seq_id());
+    os << "\"sequence_id\":" << json_escape(g_last_project_file_sequence_id);
     os << ",\"command\":\"project_file\"";
     os << param_field;
     os << ",\"project_id\":" << json_escape(opts.project_id);
@@ -442,6 +447,11 @@ std::string build_project_file_json_impl(const BBL::PrintParams& p,
 
 } // namespace
 
+std::string last_project_file_sequence_id()
+{
+    return g_last_project_file_sequence_id;
+}
+
 std::string build_project_file_json(const BBL::PrintParams& p,
                                     const ProjectFileOpts&  opts)
 {
@@ -713,3 +723,8 @@ int Agent::run_send_gcode_to_sdcard(const BBL::PrintParams& params,
 }
 
 } // namespace obn
+
+OBN_ABI std::string bambu_network_get_last_project_file_sequence_id(void*)
+{
+    return obn::print_job::last_project_file_sequence_id();
+}
diff --git a/src/print_job_naming.cpp b/src/print_job_naming.cpp
index 914ea24..c53f189 100644
--- a/src/print_job_naming.cpp
+++ b/src/print_job_naming.cpp
@@ -48,7 +48,8 @@ std::string strip_3mf_extension(std::string name)
 
 std::string pick_remote_name(const BBL::PrintParams& p)
 {
-    std::string project = p.project_name;
+    std::string project = p.ftp_file;
+    if (project.empty()) project = p.project_name;
     if (project.empty()) project = p.task_name;
     project = sanitize_remote_name(project);
     project = strip_3mf_extension(project);
